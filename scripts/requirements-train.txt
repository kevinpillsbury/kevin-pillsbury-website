# Python dependencies for training the rating head.
# Install: pip install -r scripts/requirements-train.txt
# Use a venv: python -m venv .venv && source .venv/bin/activate (or .venv\Scripts\activate on Windows)

google-genai>=1.0.0
tensorflow>=2.15.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
